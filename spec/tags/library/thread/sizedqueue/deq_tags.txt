fails:Thread::SizedQueue#deq removes an item from the Queue
fails:Thread::SizedQueue#deq returns items in the order they were added
fails:Thread::SizedQueue#deq blocks the thread until there are items in the queue
fails:Thread::SizedQueue#deq raises a ThreadError if Queue is empty
